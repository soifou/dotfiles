#!/usr/bin/env sh

if ! command -v dunstctl > /dev/null 2>&1; then
    echo Missing dunsctl, please install first.
    exit 1
fi

case "$1" in
    -t) dunstctl set-paused toggle ;;
esac

[ "$(dunstctl is-paused)" = "false" ] && echo  || echo 
