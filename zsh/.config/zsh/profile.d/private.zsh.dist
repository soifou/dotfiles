## OS Specific

# export XCURSOR_SIZE=24

# Name of the primary monitor
# Usually from xrandr | grep primary | awk '{print $1}'
# export MONITOR=

# Name of the printer
# export PRINTER=

# export BSPWM_GAPS=10
# export BSPWM_TOP_PADDING=30

# export OPENAI_API_KEY=sk-foo

# export OPENWEATHERMAP_APIKEY=
# export OPENWEATHERMAP_LAT=
# export OPENWEATHERMAP_LONG=

# Suppress warning messages from org.a11y.Bus
# See: https://issues.guix.gnu.org/28088
# export NO_AT_BRIDGE=1

# Suppress experimental warning from node
# export NODE_OPTIONS="--disable-warning=ExperimentalWarning"

# Disable OpenGL to use sofware rendering (slow)
# export LIBGL_ALWAYS_SOFTWARE=1

# Set Helix runtime dir
# https://docs.helix-editor.com/install.html#configuring-helixs-runtime-files
# export HELIX_RUNTIME=$XDG_DEVELOP_DIR/testing/helix/runtime

# See |$NVIM_APPNAME| and |$NVIM_LOG_FILE|
# export NVIM_APPNAME=nvim
# export NVIM_LOG_FILE="$XDG_STATE_HOME/$NVIM_APPNAME/nvim.log"
# export VIMINIT='let $MYVIMRC = !has("nvim") ? "$XDG_CONFIG_HOME/vim/vimrc" : "$XDG_CONFIG_HOME/$NVIM_APPNAME/init.lua" | so $MYVIMRC'

# My dotfiles absolute dir path
# export DOTS_DIR=/home/foo/dots

# Useless notifier if managed via homebrew.
# export GH_NO_UPDATE_NOTIFIER=1

# Homebrew
# export HOMEBREW_NO_ANALYTICS=1
# export HOMEBREW_NO_AUTO_UPDATE=1

# Always use Unicode line-drawing characters, not VT100-style ones
# This fix borders for some terminals on gpg-pinentry-curses
# export NCURSES_NO_UTF8_ACS=1

# Default PHP version to use with docker container
# export PHP_VERSION=8.4
# Build options
# export PHP_CONFIGURE_OPTIONS="--with-openssl --with-curl --with-zlib --with-readline --with-gettext --with-sodium"

# https://github.com/Benexl/yt-x
# export YT_X_FZF_OPTS="$FZF_DEFAULT_OPTS"
