#!/usr/bin/env sh

# Open images with sxiv from the web
# Dependencies: sxiv, curl

TMPFILE=$(mktemp)
curl -sL "$1" >"$TMPFILE"

case $(file --mime-type "$TMPFILE" -bL) in
    image/gif) sxiv -ab "$TMPFILE" ;;
    image/*) sxiv -b "$TMPFILE" ;;
    *) exit 1 ;;
esac
