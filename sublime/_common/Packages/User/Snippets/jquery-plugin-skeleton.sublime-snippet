<snippet>
    <content><![CDATA[
(function(\$) {
    \$.fn.extend({
        $1: function(options) {
            var defaults = {
                $2
            };

            options = \$.extend(defaults, options);

            // do your own logic here
            $3
        }
    });
})(jQuery);
]]></content>
    <tabTrigger>jqpl</tabTrigger>
    <scope>source.js, source.jsx, source.ts</scope>
    <description>jQuery plugin skeleton</description>
</snippet>