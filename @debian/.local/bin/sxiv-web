#!/usr/bin/env sh

# Open images with sxiv from the web
# Dependencies: sxiv, curl

TMPFILE=$(mktemp)
curl -sL "$1" >"$TMPFILE"

case $(file --mime-type "$TMPFILE" -bL) in
    image/gif)
        sxiv -a "$TMPFILE"
        ;;
    image/*)
        sxiv "$TMPFILE"
        ;;
    *) exit 1 ;;
esac
